const esc=value=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]));
const state={data:null,selectedMonth:null};
const currentMonth=new Date().toISOString().slice(0,7);
function peopleFor(status){return state.data.months.flatMap(month=>(month.presenters||[]).filter(person=>person.status===status).map(person=>({...person,month:month.month})))}
function renderTable(id,people){document.getElementById(id).innerHTML=people.length?people.map(person=>`<tr class="${person.status}"><td class="month">${esc(person.month.replace("-","."))}</td><td class="name">${esc(person.name)}</td><td>${esc(person.department||"-")}</td><td>${esc(person.jobTitle||"-")}</td></tr>`).join(""):`<tr><td class="empty" colspan="4">표시할 발표자가 없습니다.</td></tr>`}
function renderStage(){const month=state.data.months.find(item=>item.month===state.selectedMonth),stage=document.getElementById("stage");if(!month||!month.assignedCount){stage.innerHTML="아직 추첨 결과가 없습니다. 관리자 추첨 후 확인할 수 있습니다.";return}const people=(month.presenters||[]).map(person=>`<div class="stage-person"><b>${esc(person.name)}</b><span>${esc(person.department||person.jobTitle||"")}</span></div>`).join("");stage.innerHTML=state.data.cycle.showNames?`<div><strong>${esc(month.month.replace("-","."))} · ${esc(month.assignedCount)}명</strong><div class="stage-people">${people}</div></div>`:`<div><strong>${esc(month.month.replace("-","."))} · ${esc(month.assignedCount)}명</strong><span>발표자가 배정되었습니다.</span></div>`}
function showResult(){const month=state.data.months.find(item=>item.month===state.selectedMonth);if(!month||month.assignedCount!==state.data.cycle.monthlyTarget){alert(`이 달의 발표자는 ${month?.assignedCount||0}명입니다.`);return}const people=state.data.cycle.showNames?(month.presenters||[]).map(person=>`<div class="winner"><b>${esc(person.name)}</b><span>${esc([person.department,person.jobTitle].filter(Boolean).join(" · "))}</span></div>`).join(""):`<div class="winner"><b>${esc(month.assignedCount)}명</b><span>발표자 선정 완료</span></div>`;document.getElementById("result-content").innerHTML=`<div class="result-month">${esc(month.month.replace("-","."))} CM 발표자 추첨 결과</div><h2 class="result-title">🎉 축하합니다! 🎉</h2><div class="winner-grid">${people}</div>`;document.getElementById("result-dialog").showModal()}
function render(data){state.data=data;document.getElementById("cycle-label").textContent=`CM 활동 기간 ${data.cycle.startMonth.replace("-",".")} ~ ${data.cycle.endMonth.replace("-",".")} · 월 ${data.cycle.monthlyTarget}명`;const available=data.months.filter(month=>month.month<=data.cycle.planningEndMonth||month.assignedCount>0),selected=available.find(month=>month.month===currentMonth)||available.find(month=>month.month>currentMonth&&month.assignedCount>0)||available.at(-1);state.selectedMonth=selected?.month||available[0]?.month;const select=document.getElementById("month-select");select.innerHTML=available.map(month=>`<option value="${esc(month.month)}"${month.month===state.selectedMonth?" selected":""}>${esc(month.month.replace("-","."))}</option>`).join("");const summary=[["대상",data.summary.eligibleCount],["발표 완료",data.summary.completedCount],["발표 예정",data.summary.scheduledCount],["남은 인원",data.summary.remainingCount]];document.getElementById("summary").innerHTML=summary.map(([label,value])=>`<div class="summary-card"><span>${esc(label)}</span><strong>${esc(value)}</strong></div>`).join("");renderTable("completed-table",peopleFor("completed"));renderTable("scheduled-table",peopleFor("scheduled"));renderStage()}
document.getElementById("month-select").addEventListener("change",event=>{state.selectedMonth=event.target.value;renderStage()});document.getElementById("show-result").addEventListener("click",showResult);document.getElementById("result-close").addEventListener("click",()=>document.getElementById("result-dialog").close());document.getElementById("result-dialog").addEventListener("click",event=>{if(event.target.closest(".result-content"))return;document.getElementById("result-dialog").close()});fetch("data/presenter-rotation.json",{cache:"no-cache"}).then(response=>{if(!response.ok)throw new Error(response.status);return response.json()}).then(render).catch(()=>{document.getElementById("stage").textContent="발표 일정 데이터를 불러오지 못했습니다."});window.rotationPage={state,renderStage,esc};
